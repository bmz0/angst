# Angst

WebAudio synthesizer application built with Angular and Copilot (Claude Sonnet).

## Local server

To start the local server, run:

```bash
npm start
```

The synth runs at `http://localhost:4200/#/synth`.
